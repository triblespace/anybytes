# @generated by autocargo from //eden/scm/lib/minibytes:minibytes

[package]
name = "minibytes"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
bytes = { version = "1.6.0", features = ["serde"], optional = true }
memmap2 = { version = "0.9.4", optional = true }

[dev-dependencies]
quickcheck = "1.0"

[features]
default = ["frombytes", "frommmap", "non-zerocopy-into"]
frombytes = ["bytes"]
frommmap = ["memmap2"]
non-zerocopy-into = []
